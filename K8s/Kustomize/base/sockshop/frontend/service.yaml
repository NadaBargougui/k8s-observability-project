apiVersion: v1
kind: Service
metadata:
  name: front-end
  # annotations:
  #   prometheus.io/scrape: 'true'
  labels:
    name: front-end
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
  selector:
    app.kubernetes.io/name: front-end
